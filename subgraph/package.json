{
  "name": "c-demo",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ c-demo",
    "create-local": "graph create --node http://localhost:8020/ c-demo",
    "remove-local": "graph remove --node http://localhost:8020/ c-demo",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 c-demo",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.48.0",
    "@graphprotocol/graph-ts": "0.29.1",
    "ethers": "5"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
